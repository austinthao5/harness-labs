apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-flask
  labels:
    app: simple-flask
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-flask
  template:
    metadata:
      labels:
        app: simple-flask
    spec:
      containers:
        - name: flask-container
          image: austinthao/flask-app:v1
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 20   # Wait 15 seconds before starting to probe
            periodSeconds: 2          # Check every 2 seconds after that
            timeoutSeconds: 1         # Timeout for each probe
            failureThreshold: 3       # Mark pod not ready after 3 consecutive failures
            successThreshold: 1       # Mark pod ready after 1 successful probe
          ports:
            - containerPort: 80
